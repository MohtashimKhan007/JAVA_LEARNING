spring.application.name=crudSpringBootDemo
server.port = 8089
spring.datasource.url=jdbc:mysql://localhost:3306/coder
spring.datasource.username=root
spring.datasource.password=Mohtashim
# here we are saying to jpa to create the tables
spring.jpa.hibernate.ddl-auto= update

# if we want to see the sql query as well then
spring.jpa.show-sql=true
# to format the query
spring.jpa.properties.hibernate.format_sql = true
# if we want to show the exact error to the client then we use this but we dont use this in production
spring.web.error.include-message=always
